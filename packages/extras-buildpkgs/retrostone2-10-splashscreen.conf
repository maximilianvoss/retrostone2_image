# casserts
local package_name="retrostone2_splashscreen"
local package_repo="https://github.com/maximilianvoss/retrostone2_splashscreen.git"
local package_ref="branch:master"

local package_upstream_version="1.0"
local package_builddeps=""
local package_install_target="retrostone2_splashscreen"
local package_component="${release}-utils"

package_checkbuild()
{
	[[ $BOARD = retrostone2 ]]
}

package_checkinstall()
{
	[[ $BOARD = retrostone2 ]]
}

