#!/usr/bin/make -f

override_dh_auto_configure:
    apt-get update
	apt-get install -qq -y --no-install-recommends casserts
	cmake -DCMAKE_INSTALL_PREFIX=/usr

%:
	dh $@

override_dh_builddeb:
	dh_builddeb -- -Zgzip -z9

override_dh_usrlocal: